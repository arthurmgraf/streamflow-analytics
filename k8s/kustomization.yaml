apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Kafka
  - kafka/kafka-topics.yaml

  # Flink Jobs
  - flink/fraud-detector.yaml
  - flink/transaction-processor.yaml
  - flink/realtime-aggregator.yaml

  # Security
  - security/network-policies.yaml
  - security/pod-disruption-budgets.yaml

  # Monitoring
  - monitoring/service-monitors.yaml
  - monitoring/alerting-rules.yaml
  - monitoring/slo-rules.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: streamflow
